{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".output/**", "out/**"]
    },
    "lint": {},
    "format": {},
    "check": {},
    "check:ci": {},
    "test": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
