table:
  schema: public
  name: icons
select_permissions:
  - role: public
    permission:
      columns:
        - id
        - name
        - type
        - url
      filter: {}
  - role: quest_admin
    permission:
      columns:
        - id
        - name
        - type
        - url
      filter: {}
