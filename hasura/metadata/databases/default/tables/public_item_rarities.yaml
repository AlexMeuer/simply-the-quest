table:
  schema: public
  name: item_rarities
is_enum: true
array_relationships:
  - name: rewards
    using:
      foreign_key_constraint_on:
        column: rarity
        table:
          schema: public
          name: rewards
select_permissions:
  - role: public
    permission:
      columns:
        - name
        - color
      filter: {}
  - role: quest_admin
    permission:
      columns:
        - color
        - name
      filter: {}
