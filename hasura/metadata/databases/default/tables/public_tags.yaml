table:
  schema: public
  name: tags
array_relationships:
  - name: quest_tags
    using:
      foreign_key_constraint_on:
        column: tag_name
        table:
          schema: public
          name: quest_tags
select_permissions:
  - role: public
    permission:
      columns:
        - name
      filter: {}
      allow_aggregations: true
