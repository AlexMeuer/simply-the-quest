services:
  app:
    image: oven/bun:latest
    working_dir: /app
    volumes:
      - ../remix:/app
      - app_node_modules:/app/node_modules
      - bun_cache:/root/.bun
    command: sh -c "bun install && bun run dev -- --host 0.0.0.0 --port 5173"
    ports:
      - "5173:5173"
    environment:
      CHOKIDAR_USEPOLLING: "true"
      VITE_MINIO_ENDPOINT: "localhost"
      VITE_MINIO_PORT: 9000
      VITE_MINIO_ACCESS_KEY: "devminio"
      VITE_MINIO_SECRET_KEY: "devminiosecret"
      VITE_NEO4J_URI: "bolt://neo4j:7687"
      VITE_NEO4J_USER: neo4j
      VITE_NEO4J_PASSWORD: devpassword
    depends_on:
      - neo4j
      - minio
      - meilisearch
    extra_hosts:
      - "localhost:host-gateway"

  neo4j:
    image: neo4j:latest
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: neo4j/devpassword
    volumes:
      - neo4j-data:/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "cypher-shell -u neo4j -p devpassword 'RETURN 1' || exit 1",
        ]
      interval: 10s
      timeout: 5s
      retries: 5

  neo4j-init:
    image: neo4j:latest
    depends_on:
      neo4j:
        condition: service_healthy
    environment:
      NEO4J_URI: neo4j://neo4j:7687
      NEO4J_USERNAME: neo4j
      NEO4J_PASSWORD: devpassword
    volumes:
      - ./infra/neo4j.init.cypher:/init.cypher:ro
    entrypoint: ["/bin/bash", "-c"]
    command: ["cypher-shell", "-f", "/init.cypher"]

  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: devminio
      MINIO_ROOT_PASSWORD: devminiosecret
    command: server /data --console-address ":9001"
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/ready"]
      interval: 10s
      timeout: 5s
      retries: 5

  meilisearch:
    image: getmeili/meilisearch:latest
    ports:
      - "7700:7700"
    environment:
      MEILI_MASTER_KEY: devmeili
      MEILI_NO_ANALYTICS: true
    volumes:
      - meili-data:/meili_data

  # One-shot init to create bucket + enable versioning + set CORS
  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    volumes:
      - ./minio.cors.xml:/cors.xml:ro
      - ./minio.init.sh:/init.sh:ro
    entrypoint: ["/bin/sh"]
    command: "/init.sh"
    restart: "no"

volumes:
  neo4j-data:
  meili-data:
  minio-data:
  app_node_modules:
  bun_cache:
